/**
 * Public homepage routing — School Talent only at /
 */
export const homepageStrategy = {
  active: 'schoolTalent',
  routes: {
    home: '/',
    schoolTalent: '/',
    explore: '/explore',
  },
}
